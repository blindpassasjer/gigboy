services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: gigboy
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env.selfhost}
      POSTGRES_DB: gigboy
    volumes:
      - gigboy-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U gigboy -d gigboy"]
      interval: 5s
      timeout: 5s
      retries: 10

  app:
    build: .
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: ${DATABASE_URL:?Set DATABASE_URL in .env.selfhost}
      SESSION_SECRET: ${SESSION_SECRET:?Set SESSION_SECRET in .env.selfhost}
      ATTACHMENTS_DIR: /data/attachments
      PORT: ${PORT:-3000}
      NODE_ENV: production
    volumes:
      - gigboy-attachments-data:/data/attachments
    ports:
      - "${PORT:-3000}:${PORT:-3000}"

volumes:
  gigboy-postgres-data:
  gigboy-attachments-data:
